name: Lint

on:
  push:
    branches: [ "main", "gnome43-44" ]
  pull_request:
    branches: [ "main", "gnome43-44" ]
  schedule:
    - cron: '0 4 * * 1'
  workflow_dispatch:

jobs:
  eslint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Run ESLint
        run: |
          npx -y eslint@9 extension.js extension-esm.js prefs.js prefs-es6.js eslint.config.cjs
